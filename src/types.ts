export type JobStatus = 'QUEUED' | 'ASSIGNING' | 'RUNNING' | 'DOWNLOADING' | 'COMPLETED' | 'FAILED' | 'CANCELLED'
export type JobStage = 'JOB_CREATED' | 'PROFILE_ASSIGNED' | 'INPUT_UPLOADING' | 'CONTAINER_STARTING' | 'GPU_READY' | 'MODEL_LOADING' | 'MODEL_READY' | 'PREPROCESSING' | 'GENERATING' | 'ENCODING' | 'SAVING' | 'RESULT_DOWNLOADING' | 'COMPLETED'
export interface Profile { id: string; name: string; workspaceLabel: string; enabled: boolean; budgetLimit: number | null; budgetUsed: number; reserveAmount: number; maxConcurrency: number; runningJobs: number; priority: number; lastError?: string; lastUsedAt?: string }
export interface Job { id: string; profileId?: string; functionCallId?: string; status: JobStatus; stage: JobStage; prompt: string; inputPath: string; outputPath?: string; thumbnailPath?: string; duration: number; resolution: string; seed?: number; progress?: number; error?: string; createdAt: string; startedAt?: string; completedAt?: string; logs: string[] }
