{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local control plane window. Asset preview scope is defined by app.security.assetProtocol in tauri.conf.json.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
